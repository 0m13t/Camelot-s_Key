# Camelot-s_Key
An CNN-powered audio processor designed for DJs using legacy hardware (Pioneer CDJ-1000, CDJ-800, etc.). This tool analyzes your music library for Key and BPM, then injects that data directly into the file metadata and filenames so they are visible on small hardware displays.
